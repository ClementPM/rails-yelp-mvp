<h2><%= @restaurant.name %></h2>
<p>Address: <%= @restaurant.address %></p>
<p>Type of cuisine: <%= @restaurant.category %></p>
<p>Phone number: <%= @restaurant.phone_number %></p>

<%= link_to restaurant_path(@restaurant), method: :delete, data: {confirm: "Are you sure?"}, class: 'btn btn-danger' do %> 
<i class="far fa-trash-alt"></i> Delete

<% end %>

<%= link_to 'See all restaurant', restaurants_path, class: 'btn btn-secondary' %>
<%= link_to 'Add a restaurant', new_restaurant_path(@restaurant), class: 'btn btn-info' %>

<%= link_to "leave a review", new_restaurant_review_path(@restaurant), class: 'btn btn-primary' %>

<% @restaurant.reviews.each do |review| %>
  <%= review.content  %>
  <%= review.rating  %>
<% end %>


